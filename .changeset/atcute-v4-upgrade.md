---
'@svelte-atproto/sv': patch
---

Scaffold against the new atcute majors. Bumps the pinned `@svelte-atproto/oauth` to `^0.3.0` (the version that depends on atcute v4/v5) and the scaffolded `@atcute/atproto` dependency to `^4.0.0`, which keeps the lexicon augmentation aligned with `@atcute/client` v5.
